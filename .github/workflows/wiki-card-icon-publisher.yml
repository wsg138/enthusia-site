name: Publish wiki card icons

on:
  pull_request:
    paths:
      - 'wiki-card-icon-trigger.txt'

permissions:
  contents: read

jobs:
  publish:
    runs-on: ubuntu-24.04
    env:
      WIKI_API: https://enthusia.miraheze.org/w/api.php
      WIKI_FULL_BACKUP_OUT: wiki-card-icon-output/full-backup
      WIKI_ICON_OUT: wiki-card-icon-output
      WIKI_BOT_USERNAME: ${{ secrets.WIKI_BOT_USERNAME || secrets.MIRAHEZE_BOT_USERNAME || secrets.WIKI_USERNAME }}
      WIKI_BOT_PASSWORD: ${{ secrets.WIKI_BOT_PASSWORD || secrets.MIRAHEZE_BOT_PASSWORD || secrets.WIKI_PASSWORD }}
    steps:
      - uses: actions/checkout@v4
      - name: Validate icon sources
        run: |
          set -euo pipefail
          python3 -m py_compile wiki-worker/card-icons/publish.py
          for f in Commands.svg Market.svg Warzone.svg HistoryLore.svg; do
            test -s "wiki-worker/card-icons/$f"
            grep -q '#a9db7a' "wiki-worker/card-icons/$f"
          done
      - name: Capture complete live wiki before edits
        run: |
          set -euo pipefail
          mkdir -p wiki-card-icon-output
          python3 wiki-worker/full_backup.py | tee wiki-card-icon-output/full-backup.log
          test -s wiki-card-icon-output/full-backup/manifest.json
          pages="$(find wiki-card-icon-output/full-backup/pages -type f -name '*.wiki' | wc -l | tr -d ' ')"
          histories="$(find wiki-card-icon-output/full-backup/history -type f -name '*.json' | wc -l | tr -d ' ')"
          test "$pages" -gt 0
          test "$histories" -eq "$pages"
      - name: Upload immutable pre-edit backup
        uses: actions/upload-artifact@v4
        with:
          name: enthusia-wiki-card-icon-backup-${{ github.run_id }}
          path: wiki-card-icon-output/
          retention-days: 90
      - name: Publish icons and private player-link polish
        run: |
          set -euo pipefail
          test -n "$WIKI_BOT_USERNAME"
          test -n "$WIKI_BOT_PASSWORD"
          python3 wiki-worker/card-icons/publish.py | tee wiki-card-icon-output/publish.log
      - name: Upload publication evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: enthusia-wiki-card-icon-evidence-${{ github.run_id }}
          path: wiki-card-icon-output/
          if-no-files-found: warn
          retention-days: 90
